#include <stdio.h>
#include <ctype.h>
#include "constantes.h"
#include "funcoes_aulas.h"



/*
void mostrarDados(tipoEstudante turma[], int numEstudantes){
    int i;
    for(i=0;i<numEstudantes;i++){
        printf("\nNúmero: %d \t Nome: %s\n", turma[i].numero, turma[i].nome);

        if(turma[i].data != -1){
           printf("Nota: %.1f\t &d-%d-%d\n", turma[i].nota, turma[i].data.dia, turma[i].data.mes, turma[i].data.ano);
        }
        else{
            printf("Nao avaliado");
        }

    }
}*/
